## Learned User Preferences
- Prefers explicit, step-by-step operational guidance for deployment and infrastructure setup tasks.
- Prefers concrete "what to click/do next" instructions over high-level explanations when configuring GitHub, Vercel, Stripe, and Supabase.
- For recruiter-facing portfolio work, prefers a clear software-engineering headline and conventional professional tone over religious verses, polarizing personal branding, or sales-heavy CTAs; wants several strong technical projects with concrete proof (repository links, live demos, screenshots or similar visuals, metrics when applicable) and messaging that keeps engineering identity primary.
- Prefers keeping generated Cursor automation state (for example JSON under `.cursor/hooks/state/`) out of git via `.gitignore` rather than committing ephemeral tooling files.
- For portfolio updates, prefers real resume files, real GitHub-backed projects, and actual assets over placeholder screenshots or filler content.

## Learned Workspace Facts
- The user actively works across local repos including `C:\Users\User\.claude\workspaces\portfolio-website` (recruiter-focused portfolio and related showcases such as `rfid-security-toolkit/` under `C:\Users\User\.claude\workspaces\rfid-security-toolkit`), `E:\Downloads\Only-fans-SaaS-main\Only-fans-SaaS-main`, and `C:\Users\User\.claude\workspaces\only-fans-saas` (frequent primary checkout for CreatorHub / Next.js / Supabase work).
- CI/CD work in this workspace commonly involves GitHub Actions pipelines and Vercel deployment configuration.
- The `portfolio-website` repo is expected to deploy to production through Vercel's Git integration on pushes to `main`, rather than a custom GitHub Actions Vercel deploy workflow.
